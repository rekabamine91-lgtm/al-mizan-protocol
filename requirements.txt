# ============================================================================
# Al-Mizan Protocol v1.0.0 - Dependencies
# Constitutional Autograd for Digital Sovereignty
# ============================================================================
# Install with: pip install -r requirements.txt
# ============================================================================
# Supported Python versions: 3.8, 3.9, 3.10, 3.11
# Lightweight design: minimal external dependencies for edge devices
# ============================================================================

# ----------------------------------------------------------------------------
# Core Numerical Computation (engine.py)
# ----------------------------------------------------------------------------
numpy>=1.21.0,<2.0.0          # Array operations, mathematical functions, variance calculation
pandas>=1.3.0,<2.0.0          # Data management for audit trails (JSON serialization)

# ----------------------------------------------------------------------------
# Interactive Dashboard (app.py)
# ----------------------------------------------------------------------------
streamlit>=1.20.0,<2.0.0       # Main web interface for Al-Mizan dashboard
matplotlib>=3.5.0,<4.0.0       # Static plots for entropy distribution and integrity evolution
plotly>=5.17.0,<6.0.0          # Interactive charts for advanced zakat visualization (optional)

# ----------------------------------------------------------------------------
# Development & Testing (optional)
# ----------------------------------------------------------------------------
# seaborn>=0.11.0,<1.0.0       # Enhanced statistical visualizations (optional, for simulation.py)
# imageio>=2.20.0              # GIF generation for README demo (not required for runtime)

# ============================================================================
# Notes:
# - engine.py and zakat_manager.py use only Python standard library + numpy
# - No deep learning frameworks (PyTorch/TensorFlow) required – Al-Mizan is self-contained
# - Tested on: Linux, macOS, Windows, and mobile ARM devices (via Termux)
# - Total install size: ~50 MB (minimal compared to PyTorch's 2GB+)
# ============================================================================
